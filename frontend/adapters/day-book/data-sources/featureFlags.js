// frontend feature flags for day-book data sources
// flip to hide a feature from the UI without deleting code
const dataSourceFeatureFlags = {
	testConnection: false,
};

export default dataSourceFeatureFlags;
